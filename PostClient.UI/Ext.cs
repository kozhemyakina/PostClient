
namespace PostClient.UI
{
    //public static class Ext
    //{
    //    public static void DeleteMessageWithForce(this Pop3Client cl, string subj)
    //    {
    //        using (var Client = new ImapClient("imap.gmail.com", 993,
    //            Credentials.Email, Credentials.Password, AuthMethod.Login, true))
    //        {
    //            var ids = Client.Search(SearchCondition.All());
    //            foreach (var id in ids)
    //            {
    //                var m = Client.GetMessage(id);
    //                if (m.Subject == subj)
    //                    Client.DeleteMessage(id);
    //            }
    //        }
    //    }
    //}
}